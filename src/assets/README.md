# Assets

Drop image, video, or icon assets here. Import them into components like:

```jsx
import heroImage from '../assets/hero.jpg';
```

The current build uses Tailwind gradients as hero/background placeholders so
the project is self-contained out of the box.
